import React from 'react';
import { Button } from 'react-bootstrap';

const Home = () => {
    return (
        <div>
            <h2>Home </h2>
            <Button variant="primary">Primary</Button>
        </div>
    );
};

export default Home;